{
  "dependencies": {
    "body-parser": "^1.19.1",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "eslint": "^8.4.1",
    "typescript": "^4.5.3"
  },
  "scripts": {
    "start": "tsc && node --unhandled-rejections=strict ./build",
    "debug": "export DEBUG=* && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
